### **How to Run**
1. **Create a folder** named `yolo` (all lowercase) on your Desktop.  
2. **Paste the `app.py` file** inside the `yolo` folder.  
3. **Copy all files** from the `model` folder and **paste them** inside the `yolo` folder.  
4. **Create a folder** inside `yolo` named `templates` and paste `index.html` inside it.  
5. **Create another folder** inside `yolo` named `static` and paste `style.css` and `script.js` inside it.  
6. Your folder must now contain:
   - 1 Python file: `app.py`  
   - 7 YOLO model files  
   - 2 folders: `templates` and `static`

### **Run the App**
```bash
navigate to desktop 
python app.py
